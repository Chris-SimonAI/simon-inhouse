import { RestaurantDiscoveryClient } from './restaurant-discovery-client';

export const dynamic = 'force-dynamic';

export default async function RestaurantDiscoveryPage() {
  return <RestaurantDiscoveryClient />;
}

